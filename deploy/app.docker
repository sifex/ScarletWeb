
FROM php:7-fpm

RUN apt-get update && apt-get install -y libmcrypt-dev mysql-client libxml2-dev libmagickwand-dev

RUN pecl install imagick && docker-php-ext-enable imagick

RUN docker-php-ext-install mcrypt pdo_mysql xml

WORKDIR /var/www